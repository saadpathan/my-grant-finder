import { MatchBusinessRequest } from '@/types';

// Replace this URL with your deployed Lambda endpoint
/*const LAMBDA_AI_ENDPOINT = 'https://your-api-gateway-url.amazonaws.com/prod/analyzeWithAI';

export async function analyzeWithAI(profile: MatchBusinessRequest) {
  const response = await fetch(LAMBDA_AI_ENDPOINT, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(profile),
  });
  if (!response.ok) throw new Error('AI Lambda API error');
  return await response.json();
}*/
